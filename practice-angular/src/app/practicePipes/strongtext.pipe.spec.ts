import { StrongtextPipe } from './strongtext.pipe';

describe('StrongtextPipe', () => {
  it('create an instance', () => {
    const pipe = new StrongtextPipe();
    expect(pipe).toBeTruthy();
  });
});
